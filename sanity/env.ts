/*
 * The Sanity project that holds the work added in the admin. Project IDs are public
 * (they only name the project); editing needs a login invited from sanity.io/manage.
 * Empty until the project exists; the admin page then explains what to do.
 */
export const projectId = "63m9cf6q";
export const dataset = "production";
export const apiVersion = "2025-02-19";
